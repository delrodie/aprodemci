<form method="post" action="{{ path('app_producteur_delete', {'id': producteur.id}) }}" onsubmit="return confirm('Voulez-vous supprimer ce producteur?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ producteur.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
